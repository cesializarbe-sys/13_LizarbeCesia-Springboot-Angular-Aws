server:
  port: 8080

spring:
  datasource:
    url: jdbc:sqlserver://IP_SQL_SERVER:1433;databaseName=EstudiantesDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: Cesia@12345
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.SQLServerDialect